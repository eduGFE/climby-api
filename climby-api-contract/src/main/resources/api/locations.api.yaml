provinces:
    get:
      tags:
        - locations
      description: Devuelve las todas las provincias existentes.
      operationId: findAllProvinces
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../model/locations.model.yaml#/ProvinceDTO"
      deprecated: false